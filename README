# manage_externals

A handy ruby script to check out SVN externals when using git-svn to clone a subversion repo.

The externals are checkout out from SVN directly and added to your .gitignore so you won't accidentally check them in.

## Usage
ruby manage_externals.rb path/to/git/checkout/root
